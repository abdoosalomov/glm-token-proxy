{
  "name": "glm-token-proxy",
  "version": "1.0.0",
  "description": "Local proxy that handles OAuth client_credentials token refresh for the GLM API, so clients like claude-code-router can use a static dummy key.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
